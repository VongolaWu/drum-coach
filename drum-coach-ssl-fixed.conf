# Use a site-specific zone name so this config does not collide with other
# nginx vhosts that also define request limiting.
limit_req_zone $binary_remote_addr zone=drum_coach_ssl:10m rate=30r/m;

# Redirect HTTP to HTTPS
server {
    listen 80;
    server_name __DOMAIN__;
    return 301 https://$server_name$request_uri;
}

# HTTPS server
server {
    listen 443 ssl http2;
    server_name __DOMAIN__;
    
    # File upload size limit - 20MB
    client_max_body_size 20M;
    
    # SSL Configuration (managed by certbot)
    ssl_certificate /etc/letsencrypt/live/__DOMAIN__/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/__DOMAIN__/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
    
    # Security headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";

    location ^~ /.well-known/acme-challenge/ {
        root __ACME_ROOT__;
        default_type "text/plain";
        try_files $uri =404;
    }
    
    root __SITE_ROOT__;
    index index.html;

    location = /index.html {
        add_header Cache-Control "no-cache";
    }

    location / {
        limit_req zone=drum_coach_ssl burst=5 nodelay;
        try_files $uri $uri/ /index.html;
    }
}
